// @ts-check
/** @import {CandleInput} from './types.js' */
import{state,initialize,saveCandle,setView,purchase,removeCandle,changeCandle,retrySave,loadDemo}from"./state.js";import{render,byId}from"./render.js";import{validateCandleInput,statuses,lists}from"./validation.js";const dialog=/** @type {HTMLDialogElement} */byId("candle-dialog"),deletion=/** @type {HTMLDialogElement} */byId("delete-dialog"),form=/** @type {HTMLFormElement} */byId("candle-form");let editing=/** @type {string|null} */null,deleting=/** @type {string|null} */null,returnFocus="add";
/** @param {string} name */
function field(name){const el=form.elements.namedItem(name);if(!(el instanceof HTMLInputElement||el instanceof HTMLSelectElement||el instanceof HTMLTextAreaElement))throw new Error(`Missing field: ${name}`);return el}function ownedFields(){const wishlist="wishlist"===field("list").value;byId("owned-fields").hidden=wishlist,field("burnStatus").disabled=wishlist,field("rating").disabled=wishlist}
/** @param {Record<string,string>} errors */function showErrors(errors){for(const key of["name","brand","scentNotes","vesselStyle","list","burnStatus","rating"])field(key).setAttribute("aria-invalid",String(Boolean(errors[key]))),byId(`error-${key}`).textContent=errors[key]??""}
/** @param {string|null} id */function openForm(id){editing=id,form.reset(),showErrors({}),byId("form-error").textContent="";const candle=state.candles.find(c=>c.id===id);if(!id||candle){if(field("list").value=candle?.list??state.view,field("list").disabled=Boolean(candle),candle)for(const key of["name","brand","scentNotes","vesselStyle","burnStatus","rating"])field(key).value=String(candle[/** @type {keyof CandleInput} */key]??"");byId("form-title").textContent=candle?"A few finishing touches.":"Make room for a candle.",byId("save-button").textContent=candle?"Save changes":"Add candle",ownedFields(),dialog.showModal(),field("name").focus()}}function restoreFocus(){(/** @type {HTMLElement|null} */document.querySelector(`[data-focus="${returnFocus}"]`)??byId("add")).focus({preventScroll:!0})}dialog.addEventListener("close",restoreFocus),deletion.addEventListener("close",restoreFocus),field("list").addEventListener("change",ownedFields),form.addEventListener("submit",event=>{event.preventDefault();const list=lists.find(v=>v===field("list").value)??"shelf",burnStatus=statuses.find(v=>v===field("burnStatus").value)??"unlit",input={name:field("name").value,brand:field("brand").value,scentNotes:field("scentNotes").value,vesselStyle:field("vesselStyle").value,list:list,burnStatus:"wishlist"===list?"unlit":burnStatus,rating:"wishlist"!==list&&field("rating").value?Number(field("rating").value):null},result=validateCandleInput(input);if(showErrors(result.errors),byId("form-error").textContent=result.valid?"":"Check the highlighted details.",result.valid)try{const result=saveCandle(input,editing);result.ok?dialog.close():byId("form-error").textContent=`Could not save this candle: ${result.error}`}catch(error){byId("form-error").textContent=error instanceof Error?error.message:"Candle could not be updated. Try again."}else field(Object.keys(result.errors)[0]??"name").focus()}),document.addEventListener("click",event=>{const target=event.target instanceof Element?event.target.closest("button"):null;if(!(target instanceof HTMLButtonElement))return;const view=lists.find(v=>v===target.dataset.view);if(view)return void setView(view);const id=target.dataset.id;switch(target.dataset.action){case"add":returnFocus="add",openForm(null);break;case"edit":id&&(returnFocus=`edit-${id}`,openForm(id));break;case"close":dialog.close();break;case"demo":loadDemo(),byId("tab-shelf").focus();break;case"retry":retrySave();break;case"purchase":id&&(purchase(id),byId("tab-wishlist").focus({preventScroll:!0}));break;case"delete":{const candle=state.candles.find(c=>c.id===id);if(!candle)break;deleting=candle.id,returnFocus=`delete-${candle.id}`,byId("delete-detail").textContent=`“${candle.name}” will be deleted from your collection. This cannot be undone.`,deletion.showModal();break}case"cancel-delete":deletion.close();break;case"confirm-delete":deleting&&removeCandle(deleting),deletion.close(),deleting=null}}),document.addEventListener("change",event=>{const target=event.target;if(!(target instanceof HTMLSelectElement&&target.dataset.status))return;const status=statuses.find(s=>s===target.value);status&&changeCandle(target.dataset.status,{burnStatus:status})}),document.querySelector('[role="tablist"]')?.addEventListener("keydown",event=>{if(!(event instanceof KeyboardEvent&&["ArrowLeft","ArrowRight","Home","End"].includes(event.key)))return;event.preventDefault();const view="Home"===event.key?"shelf":"End"===event.key||"shelf"===state.view?"wishlist":"shelf";setView(view),byId(`tab-${view}`).focus({preventScroll:!0})}),
// ponytail: native dialogs supply Escape, focus trapping, inert background.
initialize(()=>render(state)),
/** @type {HTMLButtonElement} */byId("add").disabled=!1;